name: pace
channels:
  - conda-forge
dependencies:
  - python=3.9
  - pip
  - setuptools
  - wheel
  - ninja
  - packaging
  - numpy<2
  - pandas
  - pyyaml
  - pillow
  - pip:
      - -r requirements.txt
      - -r requirements_optional.txt
